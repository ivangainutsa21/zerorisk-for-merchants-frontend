import { groupByAsync } from 'zerorisk-for-merchants/helpers/group-by-async';
import { module, test } from 'qunit';

module('Unit | Helper | group by async');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = groupByAsync([42]);
  assert.ok(result);
});
