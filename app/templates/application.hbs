{{ember-load-remover}}

{{#if navi.isInUnauthenticatedRoute}}

	{{#pg-main class=(if navi.isInLoginRoute 'full-page-wrapper login-wrapper' 'full-page-wrapper') }}
		{{outlet}}
	{{/pg-main}}

{{else}}

	{{pg-sidebar}}

	{{#pg-main class="page-container"}}
		{{pg-header notifications=model.notifications}}
		{{outlet}}
		{{#unless navi.isInSaqEditRoute}}			
			{{pg-footer}}
		{{/unless}}
	{{/pg-main}}

{{/if}}

